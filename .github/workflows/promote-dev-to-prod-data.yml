name: Promote DEV → PROD (via Stored Procedure)

on:
  # Depende del nightly
  workflow_run:
    workflows: ["DEV Nightly load (17:15 Europe/Madrid)"]
    types: [completed]

  # Opcional: ejecución manual
  workflow_dispatch:
    inputs:
      ingestion_name:
        description: "Nombre de la ingestión definida en T_METADATA"
        required: true
        default: "1.INGESTION-TO-PROD"

permissions:
  contents: read

jobs:
  promote:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Snowflake CLI
        uses: snowflakedb/snowflake-cli-action@v2.0
        with:
          python-cache-dependency-glob: ""

      - name: Configure Snowflake connection
        shell: bash
        env:
          SF_ACCOUNT:   ${{ secrets.SNOWFLAKE_ACCOUNT }}
          SF_USER:      ${{ secrets.SNOWFLAKE_USER }}
          SF_PASSWORD:  ${{ secrets.SNOWFLAKE_PASSWORD }}
          SF_ROLE:      ${{ secrets.SNOWFLAKE_ROLE }}        # debe tener SELECT en DEV y WRITE en PROD
          SF_WAREHOUSE: ${{ secrets.SNOWFLAKE_WAREHOUSE }}
        run: |
          set -euo pipefail
          umask 077
          mkdir -p ~/.snowflake && chmod 700 ~/.snowflake
          cat > ~/.snowflake/config.toml <<EOF
          [connections.default]
          account = "${SF_ACCOUNT}"
          user = "${SF_USER}"
          password = "${SF_PASSWORD}"
          role = "${SF_ROLE}"
          warehouse = "${SF_WAREHOUSE}"
          authenticator = "snowflake"
          client_session_keep_alive = true
          EOF
          chmod 600 ~/.snowflake/config.toml
          export SF_SKIP_WARNING_FOR_READ_PERMISSIONS_ON_CONFIG_FILE=true
          snow --version
          snow sql -q "SELECT CURRENT_ACCOUNT(), CURRENT_ROLE(), CURRENT_WAREHOUSE();"

      - name: Run ingestion procedure
        shell: bash
        run: |
          set -euo pipefail
          ING="${{ github.event.inputs.ingestion_name }}"
          if [ -z "${ING:-}" ]; then ING="1.INGESTION-TO-PROD"; fi
          echo "🚀 CALL SP_RUN_INGESTION_JS('$ING')"
          snow sql -q "CALL DEV_BRONCE_CARS_PROPERTIES.CP_STG.SP_RUN_INGESTION_JS('$ING');"

      - name: Done
        run: echo "✅ Promotion DEV → PROD completada."
